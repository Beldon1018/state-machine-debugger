# Task

我们有一些业务流程用状态机描述，配置复杂以后经常出现状态永远到不了、事件触发后无路可走，或者同一事件同时满足多条转换的问题。请从零实现一个浏览器端状态机编辑和调试工具。

用户可以在画布上创建状态并连接转换，为转换设置事件名和守卫条件。除了编辑功能，我更关心它能否主动找出不可达状态、死胡同和非确定性转换。用户输入一串事件后，页面需要逐步播放状态变化，并解释每一步为什么成功或失败。

请准备一个包含上述问题的示例流程，支持撤销重做、自动布局、缩放、JSON 导入导出和本地保存。图比较大时也要方便定位问题，不要把它做成只能演示几个节点的原型。
